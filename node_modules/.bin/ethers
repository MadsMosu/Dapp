#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ethers-cli/bin/ethers" "$@"
  ret=$?
else 
  node  "$basedir/../ethers-cli/bin/ethers" "$@"
  ret=$?
fi
exit $ret
